{
  "name": "privatescribe",
  "productName": "PrivateScribe",
  "version": "0.4.0",
  "description": "Private local AI scribe — Electron shell",
  "author": "Second Path Studio (https://www.secondpath.dev)",
  "main": "electron/dist/main.js",
  "private": true,
  "scripts": {
    "electron:tsc": "tsc -p electron",
    "electron:dev": "npm run electron:tsc && electron .",
    "dev": "concurrently -k -n vite,electron -c blue,magenta \"cd frontend && npm run dev\" \"wait-on tcp:3000 && npm run electron:dev\"",
    "build:frontend": "cd frontend && npm run build",
    "build:electron": "tsc -p electron",
    "build:backend": "cd backend && . venv/bin/activate && pyinstaller privatescribe-backend.spec",
    "dist": "npm run build:frontend && npm run build:electron && dotenv -e .env.notarize -- electron-builder",
    "release": "npm run build:frontend && npm run build:electron && dotenv -e .env.notarize -- electron-builder --publish always"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "concurrently": "^9.0.0",
    "dotenv-cli": "^11.0.0",
    "electron": "^33.0.0",
    "electron-builder": "^26.8.1",
    "typescript": "^5.5.0",
    "wait-on": "^8.0.0"
  },
  "dependencies": {
    "electron-updater": "^6.8.3"
  }
}
