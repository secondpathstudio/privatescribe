<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTD/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron / V8 need JIT-capable executable memory. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- The PyInstaller-bundled backend ships its own non-Apple-signed dylibs
       (sqlcipher, torch, ctranslate2, etc.). Without this, the hardened
       runtime in the parent process refuses to launch those binaries. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Python dependencies sometimes look at DYLD_* / PYTHONPATH. -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>

  <!-- Microphone access for transcription. macOS still prompts the user
       on first use; this just declares the app needs it. -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
